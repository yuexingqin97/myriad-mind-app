# 大衍决 App — 开发任务清单

> 最后更新：2026-06-21（修为面板 + 追问笔记临时隐藏；基于源码核实）
>
> **v1.0 定位**：Windows 桌面端优先。专注"输入 → 炼化 → 生成笔记"，修为面板展示列表 + 统计。移动端、macOS/Linux 桌面端延后至 v2。
>
> **⚠️ 临时功能裁剪（2026-06-21）**：为聚焦核心炼化闭环，**修为面板** 与 **追问笔记（Q&A）** 已临时隐藏（UI 入口移除，代码保留以便恢复）：
> - **修为**：侧边栏「📊 修为」导航项已隐藏（`apps/desktop/src/components/layout/Sidebar.tsx` 注释）；`DashboardView` 仍是前端假数据，待 Phase 7 真实化（后端笔记目录扫描 + 元信息解析）后再开放。
> - **追问**：炼化页「🆕 新炼化 / 💬 追问笔记」模式切换已隐藏（`apps/desktop/src/components/input/InputView.tsx`，`mode` 恒 `"new"`）；QA 表单与 `handleQaSubmit` / `executeQa` 代码保留。
> - **恢复**：取消对应注释即可（修为 = 取消 Sidebar 注释；追问 = 加回模式切换按钮）。涉及下方「3.5 / 3.6 修为面板」「Q&A 追问模式」节。
>
> **图例**：`[x]` 完成 · `[~]` 部分完成（有基础/未闭环）· `[ ]` 未开始 · `[→ v2]` 延后

> 🆕 **2026-06-22 主线切换**：启动 **Agent 重构（目标驱动）**，取代当前管线驱动的 4 分流。决策：**完全重构**（设计 §九）+ **agent 优先、修为面板延后**。详见 [§七](#七agent-重构目标驱动--当前主线) 与 [`docs/设计文档/AI与模型/Agent 开发计划.md`](../设计文档/AI与模型/Agent%20开发计划.md)。当前进入 Phase 0（提示词共享化）。

---

## 一、管道打通（让炼化流程真正跑起来）✅

### 1.0 用户配置 Python 路径
- [x] 配置页签中增加"Python 路径"字段（类似 `FW_PYTHON` 环境变量）
- [x] 用户可手动指定路径（如安装了 faster-whisper 的 venv Python）
- [x] 留空 = 自动探测系统 Python（PATH 扫描）
- [x] Rust `deps.rs` 读取配置中的 Python 路径，校验版本 >= 3.9
- [x] 探测失败时在配置页签显示警告

### 1.1 接入 Python 脚本
- [x] 将 `D:/Project/MyClaude/myriad-mind/scripts/` 复制到 `scripts/`
- [x] 验证 6 个脚本可被 Rust `Command::new` 调用
- [x] 实现 `install_faster_whisper.py` 的 venv 自动创建逻辑

### 1.2 前端对接 Rust 后端
- [x] `api.ts` 封装所有 Tauri invoke() 调用（类型安全 + 浏览器降级）
- [x] App.tsx 启动时从 Rust 读取配置，首启自动跳转配置页
- [x] 流式事件监听封装（`tauriListen` 提取 `.payload`）
- [x] 事件命名：两个事件并存 — `pipeline-progress`（管线阶段进度，`pipeline.rs:1301`）+ `mind-stream`（DeepSeek 流式输出，`deepseek.rs` 5 处 emit，payload 区分 `delta`/`reasoning`/`usage`/`done` 子类型，前端 `api.ts:253` 监听）。未拆成 `mind-stream-delta/status/error` 三个独立事件名（单一 `mind-stream` 事件够用）
- [x] 进度条对接真实的 `PipelineProgress` 事件

### 1.3 管线编排实现
- [x] Rust `pipeline.rs` 实现 `execute_pipeline()` 主函数（已 2155 行）
- [x] 串联步骤：识别 → 依赖检查 → 下载 → 音频提取 → ASR → 关键帧 → AI 笔记 → 清理 → 完成
- [x] 每个步骤通过 `pipeline-progress` event 推送到前端
- [x] 缺失工具时优雅跳过（不崩溃）

### 1.4 系统依赖检测
- [x] Rust `deps.rs` 探测 Python / FFmpeg / yt-dlp / CUDA
- [x] 前端 `DepsPanel` 组件展示检测结果（绿/黄状态徽章）

---

## 二、安全与配置 ✅（除版本迁移外完成）

### 2.1 OS 密钥链
- [x] Windows：CredReadW/CredWriteW 读写 Credential Manager
- [x] 替换 config.rs 中的 stub 实现
- [x] 密钥链条目命名规范：`myriad-mind/{service}`
- [→ v2] macOS Keychain
- [→ v2] Linux libsecret

### 2.2 配置页签（与炼化/修为平级）
- [x] 将配置从 Modal 弹窗改为独立顶栏页签（⚙️ 配置），与 📥 炼化 / 📊 修为 平级
- [x] 首次启动检测 — 无配置标记时默认显示配置页签
- [x] 已配置时默认显示炼化页签
- [x] 配置变更保存到 Rust `config.json`（`~/.myriad-mind-app/config.json`）
- [x] 敏感字段（API Key）仅显示脱敏形式，编辑时写入 OS 密钥链

### 2.3 配置存储层
- [x] Windows：配置读写到 `~/.myriad-mind-app/config.json`
- [x] 原子写入（tmp + rename）
- [x] `is_first_launch()` 首启检测
- [ ] 版本号 + 迁移逻辑（`version: 1` → 后续升级）— 字段已预留，迁移函数未写
- [→ v2] 移动端配置存储

### 2.4 API Key 管理
- [x] DeepSeek API Key — 从密钥链读取（`engine.rs:23-31`：先读 config 兜底，再 `cred_read("deepseek-api-key")`），前端只显示脱敏形式
- [→ v2] Claude API Key — 可选兼容/备用，不作为 v1 必填项
- [x] AI Douyin API Key
- [x] TikHub Token
- [x] 火山引擎 VC Token

### 2.5 AI / MindEngine 接入
- [x] `DeepSeekClient`：OpenAI-compatible chat completions + stream（`ai/deepseek.rs`）
- [x] `MindEngine`：统一 Prompt、模型调用、事件推送（`ai/engine.rs`）
- [~] `TaskProfile`：由 `estimator.ts` 输出，前端 InputView 已展示灵力消耗等级；模型路由（Pro/Flash 选择）部分接入（轻量任务走 Flash）
- [x] DeepSeek V4 Pro：长上下文笔记生成
- [x] DeepSeek V4 Flash：标题、资源推荐、Vision 补全等快速任务（`deepseek.rs:26` / `engine.rs:443,464`）
- [x] `reasoning_content` 与正文分流（不写入最终笔记）

---

## 三、桌面端功能完善 ⏳（炼化闭环 ✅，修为面板 ❌）

### 3.1 视频处理管线 ✅
- [x] B 站视频：AI Douyin 解析 → 下载 → 提取音频 → ASR → 笔记（提交 `b91a6df`）
- [x] YouTube：yt-dlp 优先字幕 → 无字幕则下载 + ASR
- [x] 抖音/小红书：AI Douyin 解析 → 下载 → ASR → 笔记
- [x] yt-dlp `--dump-json` 元信息预取

### 3.2 文章处理管线 ✅
- [x] Rust 原生抓取文章内容（`fetch.rs`，dom_query + reqwest，替代 WebFetch）
- [x] 平台识别（知乎/CSDN/掘金/Wiki/微信等）+ 反爬降级（知乎/微信→友好错误+替代方案）
- [x] HTML→纯文本提取（平台专用 CSS 选择器 + 通用回退）+ 语言检测
- [x] MindEngine 调用 DeepSeek V4 Pro 生成结构化学习笔记

### 3.3 本地文件处理 ⏳
- [x] 本地视频：提取音频 → ASR → 关键帧 → 笔记
- [x] 本地音频：ASR → 笔记
- [x] 本地文档（.md/.txt）：直接读 → DeepSeek 分析 → 笔记
- [ ] 本地目录：扫描 → 逐个处理 → 合并笔记（批量处理未做）
- [ ] 本地 .html 文件：知乎/微信保存的 HTML 提取（未做）

### 3.4 代码项目分析 ⏳
- [ ] GitHub URL → git clone → 扫描结构 → DeepSeek 分析 → 架构笔记（未做）
- [x] 本地代码目录扫描（`code_project.rs`：递归遍历 + 5 级文件优先级 + 32KB/512KB 限制 + 技术栈推断 + 目录树）
- [x] 灵力评估（`estimateCost` + 文件数分级，前端展示消耗等级）
- [x] 代码模式专用提示词（8 章报告结构 + Mermaid 架构图/依赖图）

### 3.5 笔记列表展示（修为面板中）❌ — **当前最大缺口**
- [ ] 扫描 `note_dir` 下所有 .md，解析元信息 — **后端无对应命令**（仅有通用 `fs::scan_directory`，无笔记专用扫描/解析）
- [ ] 在修为面板展示笔记列表（标题/日期/类型/难度/阅读时长）— **前端 `DashboardView.tsx` 写死 15 条 `REAL_NOTES` 常量**
- [ ] 点击笔记 → 用系统默认编辑器打开 — **`onOpenNote` 仅 `console.log`，TBD**
- [→ v2] SQLite FTS5 全文搜索
- [→ v2] 内嵌 Mermaid 图表渲染
- [→ v2] 截图资产预览

### 3.6 修为面板 ❌
- [ ] 扫描笔记目录统计 — 未接真实数据（`computeStats` 后端未实现）
- [ ] 境界进度 + 成就判定 — 逻辑在 `@myriad-mind/core`（`calculateCultivation`/`checkAchievements`）已就绪，但喂的是假数据
- [ ] 连续学习天数计算
- [ ] 知识图谱（标签关系图）
- [ ] 面板持久化到 `修为面板.md`
- [ ] 刷新按钮、打开笔记目录按钮 — 仅 `console.log`

### 附：已完成的增强能力（不在原清单但已实现）
- [x] DeepSeek Vision 关键帧审查（`ai/vision.rs`，提交 `4d084e6`）— 截图评分/选择/跳过
- [x] Q&A 追问模式 — 对已有笔记提问（提交 `4d2f568`）
- [x] `.myriad-mind/` 知识库索引 + 指纹去重（`library.rs`，提交 `e74f11b`）
- [x] 笔记自动归类 + 平铺目录 + 底部元信息块（提交 `4763ec7`/`5e70a04`）
- [x] 增量更新已有笔记 + 合并已有笔记上下文（提交 `1315c0d`）
- [x] "本次要求"提示词 Hook + memory.md 注入上下文（提交 `c56aaa4`）
- [x] 4 种内容模式契约提示词（视频/代码/网页/文档，`engine.rs content_mode_prompt`）

---

## 四、移动端功能完善 [→ v2]

> ⚠️ v1.0 仅 Windows 桌面端。移动端全部延后至 v2。

### 4.1 文章处理 [v2]
- [ ] fetch URL → 提取正文 → DeepSeek / 兼容模型 → 写 .md

### 4.2 笔记列表展示 [v2]
- [ ] 扫描笔记目录，展示列表

### 4.3 修为面板 [v2]
- [ ] 与桌面端共享 `@myriad-mind/core`

---

## 五、构建与分发 ⏳（基础配置起步）

### 5.1 桌面端打包
- [~] Windows：`tauri.conf.json` 已有 `bundle` 配置（`targets: all` + 图标 + 打包 scripts/*.py），**但未实际产出过 `.msi`/`.exe`，未验证打包链路**
- [ ] 自动更新：`tauri-plugin-updater` + GitHub Releases（未接入）
- [→ v2] macOS (.dmg)
- [→ v2] Linux (.AppImage/.deb)

### 5.2 移动端打包 [→ v2]
- [ ] Android / iOS / 鸿蒙 — 全部延后

### 5.3 CI/CD
- [ ] GitHub Actions：push main → lint + test + build
- [ ] release tag → 全平台构建 → GitHub Releases 发布

---

## 六、品质与文档 ⏳（测试全缺）

### 6.1 测试
- [ ] `packages/core` 单元测试（vitest）— **当前零测试**
- [ ] `packages/ui` 组件测试
- [ ] Rust `error.rs` / `commands/` 单元测试
- [ ] E2E 测试（Tauri + Playwright）

### 6.2 文档
- [~] README 安装说明（各平台依赖要求）— 已有基础版，缺 DeepSeek API Key 申请、faster-whisper 安装等细节
- [x] README 顶部免责声明
- [ ] 用户文档：如何申请各 API Key（AI Douyin / TikHub / 火山引擎 / DeepSeek）
- [ ] 贡献指南 CONTRIBUTING.md

### 6.3 法律合规
- [x] README 顶部免责声明
- [x] MIT 许可证保留原作者版权
- [x] 不内置任何 API Key / Token
- [x] 不提供分享/发布功能

---

## 七、Agent 重构（目标驱动 · 当前主线）

> 启动 2026-06-22。把管线驱动（4 分流）演进为目标驱动 Agent（DeepSeek tool use loop，AI 在六阶段骨架内自主选工具）。架构决策见 [`docs/设计文档/AI与模型/Agent架构设计.md`](../设计文档/AI与模型/Agent架构设计.md)，**可执行任务清单见 [`docs/设计文档/AI与模型/Agent 开发计划.md`](../设计文档/AI与模型/Agent%20开发计划.md)**。
> 推进策略：**完全重构**（Phase 0→3 连续，Phase 2-3 期间炼化可能短暂不可用，开发期可接受）。修为面板（§3.5/§3.6）延后。

### 7.0 Phase 0 — 提示词共享化 ~2-3天
- [ ] `.md` 提示词搬到 `packages/core/prompts/`，改 `prompt_manager.rs` 路径解析 + `tauri.conf.json` resources glob
- [ ] 删 `packages/core/src/prompts/*.ts` 死代码 + 清 `packages/core/src/index.ts` 的 prompts re-export 块
- [ ] 验收：视频/文章/代码三类输入炼化正常，prompt 从 core 加载，typecheck + cargo build 通过

### 7.1 Phase 1 — 工具封装 ~4-6天
- [ ] 新建 `commands/tools/`：`ToolSpec` + `ToolHandler` trait + `ToolOutput`(artifact 化) + `ToolRegistry`
- [ ] 从 `pipeline.rs` 提取媒体逻辑到 `commands/tools/media/`（下载/音频/截图内部函数），pipeline 改调用共享模块
- [ ] 封装 12 个工具（fetch_url / download_video / extract_audio / transcribe_asr / download_subtitles / scan_code_project / read_file / scan_directory / query_ai_douyin / extract_keyframes / review_keyframes / write_note）
- [ ] 验收：工具独立可调，大文本 artifact 化（只回摘要+引用），pipeline 回归不破坏

### 7.2 Spike — DeepSeek tool use 契约验证（Phase 2 闸门）
- [ ] 摸清流式/非流式、`tool_calls` 字段、reasoning 共存、tool_result 回喂格式
- [ ] 3 工具最小 loop 跑通；产出验证记录；据此定 Phase 2 改造方案（不稳则回 §一.6 重新确认）

### 7.3 Phase 2 — Agent Loop + ContextManager ~6-10天
- [ ] `deepseek.rs` 加 tool use；`agent/loop.rs` 主循环 + 护栏（步数/超时/花费）+ 取消 + 错误回喂
- [ ] `agent/context.rs`：TaskState + ArtifactStore + 阶段边界清零（§6.6）
- [ ] `agent/phases.rs`：六阶段骨架（回忆→获取→理解→生成→自检→沉淀）+ charter prompt + 输出契约校验 + generation trace + 全链路埋点
- [ ] 验收：4 类输入 agent 自主跑通，上下文不暴涨，护栏/取消有效

### 7.4 Phase 3 — 接线删旧 ~2-3天
- [ ] `execute_pipeline` 改调度 agent，删 `run_*_pipeline` 4 分流
- [ ] 重构 `pipeline-progress` 事件 + 前端 `usePipeline.ts` 适配；`mind-stream` 不变
- [ ] 成本预估确认接入；更新 `docs/架构与结构.md` §五/§六/§七
- [ ] 验收：agent 唯一路径，4 类输入全通，`pipeline.rs` 无死代码

---

## 进度总览

| 阶段 | 状态 | 完成情况 |
|------|------|----------|
| 项目脚手架 | ✅ 完成 | Monorepo + core + ui + apps/desktop |
| `@myriad-mind/core` 核心逻辑 | ✅ 完成 | classifier/estimator/panel-calc/note-parser/prompts |
| `@myriad-mind/ui` 组件库 | ✅ 完成 | ConfigWizard/Dashboard/common |
| Desktop 前端页面 | ✅ 完成 | 炼化/修为/配置三页 + 日志面板 |
| Desktop Rust 后端 | ✅ 完成 | commands/ 全模块（pipeline 2155 行）|
| CC Switch 暗色风格重构 | ✅ 完成 | |
| 一、管道打通 | ✅ 完成 | Python/脚本/IPC/管线/依赖检测 |
| 二、安全与配置 | ✅ 完成（版本迁移除外）| 密钥链/配置存储/API Key/DeepSeek+MindEngine |
| 三、桌面端功能完善 | ⏳ 炼化闭环 ✅ / 修为面板 ❌ | 视频/文章/本地/代码管线全通；修为面板仍硬编码假数据 |
| 四、移动端功能完善 | 🔒 v2 | |
| 五、构建与分发 | ⏳ 起步 | bundle 配置在，未产出安装包，无自动更新/CI |
| 六、品质与文档 | ⏳ 起步 | 零测试，README 有基础版，缺用户文档 |
| 七、Agent 重构 | 🚧 启动（主线） | 完全重构；Phase 0 待开工（见 [Agent 开发计划](../设计文档/AI与模型/Agent%20开发计划.md)） |

---

## 当前里程碑

```
Windows v1.0 ─── 一、管道打通 ✅ ─── 二、安全配置 ✅ ─── 三、功能完善 ⏳
                                                          │
                            炼化闭环 ✅ (视频/文章/本地/代码 + Vision审查 + Q&A + 知识库索引)
                                                          │
                  七、Agent 重构 🚧 (Phase 0 → 1 → Spike → 2 → 3)  ← 当前主线
                            策略：完全重构；修为面板 ❌ 延后
                                                          │
                            五、打包 ⏳  六、测试文档 ⏳
                                                          │
v2 ─── 移动端 ─── macOS/Linux ─── 搜索/对比/批量 ────────── ✗
```

## 下一步优先级（建议）

> 2026-06-22 更新：Agent 重构成为主线，优先级相应调整。

1. **🔴 Agent 重构（Phase 0→3）** — 见 [§七](#七agent-重构目标驱动--当前主线) 与 [Agent 开发计划](../设计文档/AI与模型/Agent%20开发计划.md)。**当前主线**，从 Phase 0（提示词共享化）开工。
2. **🟠 Phase 7 真实修为面板** — 原 #1，agent 主线期间**延后**（与管线无关，必要时可穿插并行）。
3. **🟡 Phase 9 打包验证** — 真正产出一次 `.msi`，跑通 `pnpm tauri build`。
4. **🟢 Phase 8 知识库增强 + 基础测试 + 用户文档**。

## 暂缓范围（v2）

- 移动端 / macOS / Linux 桌面打包
- 完整多模型 Provider 大全（Claude 等兼容模型）
- MCP / 工具调用 Agent Loop / 子智能体
- SQLite FTS5 高级搜索 / 复杂知识图谱编辑器
- 评论区精华提取 / 自动翻译流程 / 目录批量 / GitHub clone（这些 P2/P3 未做，见 `[[skill-app-parity]]` 记忆）
